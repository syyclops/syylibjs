import { variants } from "..";
import { SizeProps } from "../../types";

const sizes: SizeProps = {
  xs: "w-3 h-3 text-xs",
  sm: "w-4 h-4 text-sm",
  md: "w-5 h-5 text-lg",
  lg: "w-6 h-6 text-2xl",
};

export { variants, sizes };
