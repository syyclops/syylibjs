import { variants } from "..";
import { SizeProps } from "../../types/badge";

const sizes: SizeProps = {
  xs: "text-[0.55rem] leading-[0.7rem]",
  sm: "text-[0.6rem] leading-[0.85rem]",
  md: "text-[0.68rem] leading-[1rem]",
  lg: "text-[0.75rem] leading-[1.15rem]",
};

export { variants, sizes };
